{
  "name": "RetentionBonus",
  "type": "object",
  "properties": {
    "client_id": {
      "type": "string",
      "description": "ID of the client"
    },
    "client_name": {
      "type": "string",
      "description": "Client name for display"
    },
    "am_email": {
      "type": "string",
      "description": "Account Manager email"
    },
    "pm_email": {
      "type": "string",
      "description": "Project Manager email"
    },
    "am_bonus_amount": {
      "type": "number",
      "default": 100,
      "description": "AM bonus in dollars"
    },
    "pm_bonus_amount": {
      "type": "number",
      "default": 50,
      "description": "PM bonus in dollars"
    },
    "min_contract_months": {
      "type": "number",
      "description": "Original minimum term in months"
    },
    "renewal_month": {
      "type": "number",
      "description": "Which month of service this bonus is for (e.g. 4 means the 4th month)"
    },
    "renewal_due_date": {
      "type": "string",
      "format": "date",
      "description": "The date this renewal month payment is due"
    },
    "contract_end_date": {
      "type": "string",
      "format": "date",
      "description": "Original contract end date"
    },
    "detected_date": {
      "type": "string",
      "format": "date",
      "description": "When the renewal was detected"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "approved",
        "denied"
      ],
      "default": "pending",
      "description": "Admin approval status \u2014 pending means awaiting payment confirmation"
    },
    "reviewed_by": {
      "type": "string",
      "description": "Admin who approved/denied"
    },
    "reviewed_date": {
      "type": "string",
      "format": "date-time"
    },
    "admin_notes": {
      "type": "string"
    }
  },
  "required": [
    "client_id",
    "client_name",
    "status"
  ]
}